- name: catalogue
  hosts: all
  become: true
  vars:
    component=catalogue
  tasks:
    - name: import func_nodejs
      ansible.builtin.import_tasks: nodejs.yml











# component=catalogue
# source common.sh
# func_nodejs
# dnf install mongodb-org-shell -y
# mongo --host mongodb.linuxchunk.online </app/schema/catalogue.js


